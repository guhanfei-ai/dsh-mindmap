# 2026-08-23

## dsh-mindmap 脑图面板修复检查

- 用户要求按清单检查两个面板问题的修复：递归解析 code 工具 subCalls 中的 mindmap 结果；rename 后 latestOpeningPath 迁移。
- 检查结论：工作树中已有未提交修改（src/、scripts/、client.js、test/client.test.js 等）已完整实现两项修复，本次会话未改任何代码，仅做验证。
- 关键实现位于 `src/client/core/documents.js` 的 `reduceDocuments()`：`replayNode` 递归遍历 subCalls（WeakSet visited + MAX_SUBCALL_DEPTH=100 防死循环）；renamedFrom === latestOpeningPath 时迁移路径并保留 openingEventKey。
- `TOOL_NAMES`/`OPENING_OPS` 定义在 `src/client/runtime/open.js`（OPENING_OPS = create/open）。
- 构建流程：`npm run build:client`（scripts/build-client.mjs）将 12 个 src/client 片段拼成 client.js；构建前后 SHA-256 一致（d27ae153...）证明 client.js 与源片段同步。
- 验证结果：node --check 全过；npm test 50/50 通过（客户端 28 + 服务端 22），包含全部 5 个要求的回归测试。
- 项目约定：client.js 是生成文件，必须改 src/client 后跑 build:client；不要 git reset/commit/push（用户明确要求）。
